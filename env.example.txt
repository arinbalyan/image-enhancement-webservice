# Environment Configuration for Image Enhancement Web Service

# ===========================================
# HuggingFace Spaces Deployment Settings
# ===========================================

# GPU Settings
ENABLE_GPU=True
CUDA_VISIBLE_DEVICES=0

# API Settings
API_HOST=0.0.0.0
API_PORT=7860
DEBUG=False

# Processing Settings
MAX_IMAGE_SIZE_MB=50
BATCH_SIZE=4

# Algorithm Settings
DEFAULT_UPSCALE_FACTOR=auto
DEFAULT_QUALITY=high

# Model Paths (relative to project root)
REAL_ESRGAN_MODEL=models/RealESRGAN_x4plus.pth
GFPGAN_MODEL=models/GFPGANv1.3.pth

# Super-Resolution Settings
REAL_ESRGAN_SCALE=4
REAL_ESRGAN_TILE_SIZE=256
REAL_ESRGAN_PRE_PAD=10

# Low-Light Enhancement Settings
CLAHE_CLIP_LIMIT=2.0
CLAHE_TILE_GRID_SIZE=8

# Color Correction Settings
WHITE_BALANCE_ENABLED=True
EXPOSURE_CORRECTION_ENABLED=True

# Face Enhancement Settings
FACE_ENHANCEMENT_ENABLED=True
GFPGAN_SCALE=2

# Google Drive Settings (for local processing)
GOOGLE_DRIVE_CREDENTIALS_PATH=credentials.json
GOOGLE_DRIVE_TOKEN_PATH=token.json
INPUT_FOLDER=raw_images
OUTPUT_FOLDER=image-enhancer

# Logging Settings
LOG_LEVEL=INFO
LOG_FORMAT=json
LOG_DIR=logs

# HuggingFace Spaces Specific
HF_SPACE_NAME=img-enhance-service
HF_SPACE_SDK=docker
HF_SPACE_HARDWARE=a10g-small
HF_SPACE_SECRETS=HF_TOKEN

# ===========================================
# Notes
# ===========================================
# - Copy this file to .env and fill in your values
# - Never commit .env to version control
# - For HuggingFace Spaces, set secrets in Space settings
# - ENABLE_GPU=True recommended for HuggingFace Spaces with GPU
# - MAX_IMAGE_SIZE_MB=50 is reasonable for HuggingFace's 16GB RAM
